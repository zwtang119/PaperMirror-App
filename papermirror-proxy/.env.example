# PaperMirror 代理服务环境变量配置

# Gemini API 密钥 (必需)
GEMINI_API_KEY=your-gemini-api-key-here

# 服务端口 (可选，默认: 8080)
PORT=8080

# Node.js 环境 (可选，默认: production)
NODE_ENV=production

# 速率限制配置 (可选)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS 配置 (可选)
CORS_ORIGIN=*